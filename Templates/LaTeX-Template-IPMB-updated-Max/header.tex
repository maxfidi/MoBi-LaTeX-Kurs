%! TEX root=../main.tex
\usepackage[greek, ngerman, main=ngerman]{babel}
\usepackage[utf8]{inputenc}
%\usepackage[F1]{fontenc}
\usepackage{lmodern} % Schriftart LatinModern Vektorisiert für schöneres PDF
\usepackage[onehalfspacing]{setspace} % Zeilenabstand nach IPMB Vorgaben.
\usepackage[version=4]{mhchem}      % Chemische Formeln
\usepackage{booktabs}               % für schöne Linien in Tabellen
\usepackage{siunitx}                % Si Einheiten
\usepackage{longtable}              % Tabelle mit Seitenumbrüchen
\usepackage{makecell}               % Allow multi column cells in tables.
\usepackage{multirow}               % Mehrere Reihen in einer Tabelle zusammenfassen
\usepackage{rotate}                 % Rotate table cell content
\usepackage{acronym}                % Abkürzungen
\usepackage{lscape}                 % If need landscape pages
\usepackage{url}
	\urlstyle{same}

%%% Bibliographie mit BibTeX %%%
\usepackage{natbib}
\setlength{\bibhang}{0em}
\bibliographystyle{bibstyle/ipmbCell}

\usepackage{bibentry}
\nobibliography*


\usepackage{subfigure}              % für mehrere Unterabbildungen in einer Abbildung
\usepackage{graphicx}               % für Größenänderungen für Grafiken
\usepackage{rotating}               % für Tabellen im Querformat
\usepackage{amsmath}                % für Mathe
\usepackage[left=2.50cm, right=2.50cm, top=2.50cm, bottom=2.50cm]{geometry}    % Seitenränder nach Vorgabe von IPMB
\usepackage{lettrine}


%\usepackage{blindtext}             % Blindtext zum Testen
\usepackage{lipsum}                 % Better blindtext package

\usepackage[hidelinks]{hyperref}    % Hyperlinks [hidelinks] um linkboxen zu deaktivieren

\usepackage{scrhack}                % Anpassung von anderen Paketen an KOMA-Script zur besseren Kompatibilität

% Additional referencing commands, to make figure/table/eqution referencing the same everywhere.
% For free insertion:
\newcommand*{\Tref}[1]{Table~\ref{#1}}
\newcommand*{\Fref}[1]{Figure~\ref{#1}}
\newcommand*{\Eref}[1]{Equation~\ref{#1}}
% For reffing in Text:
\newcommand*{\sTref}[1]{(see \Tref{#1})}
\newcommand*{\sFref}[1]{(see \Fref{#1})}
\newcommand*{\sEref}[1]{(see \Eref{#1})}

\emergencystretch=50pt                  % Vergrößert Wortzwischenraum notfalls, wenn sonst die Zeile überläuft


\clubpenalty = 10000                    % Verhindern von Schusterjungen
\widowpenalty = 10000                   
\displaywidowpenalty = 10000            % und shit vor Formeln


\setkomafont{title}{\sffamily}                    % Festsetzen der Überschriftart auf Sans Serif
\setkomafont{disposition}{\usekomafont{title}}    % Festsetzen der weiteren Unterüberschriften auf Sans Serif
\setkomafont{author}{\usekomafont{title}}
\setkomafont{date}{\usekomafont{title}}